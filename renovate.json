{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", ":enableVulnerabilityAlerts", ":dependencyDashboard"],
  "baseBranches": ["main"],
  "rangeStrategy": "pin",
  "labels": ["dependencies"],
  "reviewersFromCodeOwners": true,
  "assigneesFromCodeOwners": true,
  "enabledManagers": ["npm", "corepack", "github-actions"],
  "postUpdateOptions": ["yarnDedupeHighest"],
  "packageRules": [
    {
      "matchDatasources": ["node-version"],
      "allowedVersions": "24.x",
      "description": "Node stays on the 24 LTS line; the exact patch is decided in docs/adr/0007-toolchain-node-yarn.md and verified by scripts/check-toolchain.mjs."
    },
    {
      "matchManagers": ["corepack"],
      "matchPackageNames": ["yarn"],
      "description": "Yarn patch updates land only through reviewed dependency PRs; the decision record marker in docs/adr/0007 must be updated together."
    },
    {
      "matchManagers": ["github-actions"],
      "description": "Keep third-party action SHA pins current; scripts/check-workflows.mjs requires immutable 40-hex pins."
    }
  ]
}
