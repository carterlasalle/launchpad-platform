Apply migrations in lexical order with Wrangler against the configured D1 database. Migrations are forward-only; validate on a disposable database before production. The application layer never stores secret values; only fingerprints and redacted metadata are allowed.
